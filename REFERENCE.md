# Reference

**Generated by `make reference`. Do not edit.**

A hand-maintained index is wrong the first time somebody adds an endpoint
and forgets, and a reference that might be wrong is worse than none — you
stop checking the code and start trusting it. Everything below is read out
of the code, so it cannot drift without the check in CI failing.

## Modules

| Module | State |
|---|---|
| M0 — Foundation: vocabulary, ports, layered snapshot | **done** |
| M1 — Snapshot: schema, holder, atomic swap, N−1 rollback, file source | **done** |
| M2 — Data-plane vertical slice: key auth, `/v1/chat/completions`, echo provider | **done** |
| M3 — `ProviderPort` for real: OpenAI-compatible adapter, SSE streaming, `SecretsPort` | **done** |
| M3b — Anthropic adapter and `/v1/messages` | **done** |
| M4 — Usage events, `TelemetryPort`, Prometheus, cost attribution | **done** |
| M4b — OTel spans and OTLP export | **done** |
| M5a — Control plane: domain model, snapshot builder, `gatewayctl` | **done** |
| M5b — Postgres schema, migrations, repository | **done** |
| M5c — Admin API, key rotation | **done** |
| M5d — Snapshot subscriber: worker fetches from the control plane | **done** |
| M6a — Rate limits: local lease bucket, enforced at admission | **done** |
| M6-pricing — Token classes and cost/price separation | **done** |
| M6b — Redis `KVStore`, making limits fleet-wide | **done** |
| M6c — Budgets: accounting consumer folds spend into snapshots | **done** |
| M7a — Router: selection/execution split, circuit breakers, passive health | **done** |
| M7b — Active health probing, cost and locality objectives | **done** |
| M8a — `GuardrailPort`: chain, budgets, secret scan, injection alerts | **done** |
| M8b — Policy engine: compiled decision table, geo and IP rules | **done** |
| M9a — PII chain: detection, redact/hash/tokenize, streaming restoration | **done** |
| M9b — NER sidecar for names, locations and organisations | **done** |
| M10a — Component registry: manifests, admission records, binding gate | **done** |
| M10b — Sandboxed contract-test runner and the sidecar protocol | **done** |
| M10c — WASM in-process components | **done** |
| M10d — Signed manifests | **done** |
| M11a — Fine-tuning: TrainerPort, FineTuneJob reconciler | **done** |
| M11b — Eval gate: EvalPort, scorecards, promotion gate | **done** |
| M11c — Weighted rollout: canary steps, multi-LoRA routing | **done** |
| M11d — Shadow traffic: mirrored scoring, automatic promotion | **done** |

## Admin API

Every route on the admin application, read from the app object rather than
matched with a regex — decorators that wrap across lines are exactly how an
endpoint goes missing from a list like this.

| Method | Path |
|---|---|
| `GET` | `/dashboard` |
| `GET` | `/healthz` |
| `PUT` | `/v1/aliases/{name}` |
| `PUT` | `/v1/budgets/{budget_id}` |
| `GET` | `/v1/components` |
| `POST` | `/v1/components` |
| `DELETE` | `/v1/components/{name}/{version}` |
| `GET` | `/v1/components/{name}/{version}` |
| `POST` | `/v1/components/{name}/{version}/admissions` |
| `GET` | `/v1/deployments` |
| `DELETE` | `/v1/deployments/{deployment_id}` |
| `PUT` | `/v1/deployments/{deployment_id}` |
| `GET` | `/v1/finetune/jobs` |
| `POST` | `/v1/finetune/jobs` |
| `GET` | `/v1/finetune/jobs/{tenant}/{name}` |
| `POST` | `/v1/finetune/jobs/{tenant}/{name}/cancel` |
| `POST` | `/v1/finetune/jobs/{tenant}/{name}/rollout` |
| `POST` | `/v1/finetune/jobs/{tenant}/{name}/rollout/abort` |
| `POST` | `/v1/finetune/jobs/{tenant}/{name}/rollout/advance` |
| `DELETE` | `/v1/keys/{key_id}` |
| `POST` | `/v1/keys/{key_id}/rotate` |
| `GET` | `/v1/policy` |
| `PUT` | `/v1/policy` |
| `GET` | `/v1/requests` |
| `GET` | `/v1/requests/summary` |
| `GET` | `/v1/requests/{request_id}` |
| `POST` | `/v1/snapshots` |
| `GET` | `/v1/snapshots/current` |
| `POST` | `/v1/tenants/{tenant_id}/keys` |
| `PUT` | `/v1/tenants/{tenant}` |
| `GET` | `/v1/tenants/{tenant}/policy` |
| `PUT` | `/v1/tenants/{tenant}/policy` |

## Data plane (Go)

| Package | What it is |
|---|---|
| `internal/adapters/anthropic` | implements ProviderPort for the Anthropic Messages API |
| `internal/adapters/echo` | implements ProviderPort against nothing at all: it returns the |
| `internal/adapters/guardrailsidecar` | speaks to an out-of-process guardrail over a Unix |
| `internal/adapters/injectionheuristics` | looks for prompt-injection attempts and says so |
| `internal/adapters/memkv` | is an in-process KVStore |
| `internal/adapters/nersidecar` | detects names, locations and organisations by asking a |
| `internal/adapters/openaicompat` | implements ProviderPort for anything speaking the |
| `internal/adapters/rediskv` | implements KVStore over Redis, which is what makes rate |
| `internal/adapters/redisstream` | publishes usage events to a Redis stream |
| `internal/adapters/secretscan` | refuses payloads containing credentials |
| `internal/adapters/wasmguardrail` | runs a guardrail as a WASM module in the worker's own |
| `internal/admission` | runs a port's contract suite against a component in a |
| `internal/config` | reads process settings from the environment |
| `internal/contracts` | holds the per-port contract suites: a fixed battery that |
| `internal/core` | holds the gateway's domain vocabulary: the types, errors and port |
| `internal/gateway` | is the request path: authenticate, admit, route, adapt |
| `internal/guardrails` | runs the inspections a tenant has bound, under the budget |
| `internal/httpapi` | is the HTTP transport for the data plane |
| `internal/limits` | enforces rate limits at admission |
| `internal/pii` | detects and replaces personal data before it leaves the trust |
| `internal/policy` | evaluates the compiled decision table a snapshot carries |
| `internal/router` | turns a model name into a served response |
| `internal/sandbox` | runs an untrusted component under a container runtime with |
| `internal/secrets` | resolves a deployment's credential reference into the secret |
| `internal/shadow` | mirrors real requests to an adapter nobody is serving from, |
| `internal/snapshot` | holds the worker side of snapshot distribution: the Holder |
| `internal/telemetry` | carries events off the request path |
| `internal/tracing` | configures OpenTelemetry for the worker |
| `internal/wasm` | runs an untrusted component inside the worker's own process |
| `internal/wire` | converts between the snapshot wire format and the domain types |

## Control plane (Python)

| Module | What it is |
|---|---|
| `accounting_main.py` | Run the accounting consumer |
| `admin_main.py` | Run the admin API |
| `api/app.py` | The admin FastAPI application |
| `api/dashboard.py` | The traffic dashboard, as one self-contained page |
| `api/idempotency.py` | Replay protection for mutations |
| `cli.py` | Command-line entry points for the control plane |
| `config.py` | Process configuration |
| `contracts/evaluator.py` | The contract every EvalPort must satisfy |
| `contracts/trainer.py` | The contract every TrainerPort must satisfy |
| `db/models.py` | SQLAlchemy models: the control plane's source of truth |
| `db/repository.py` | Read the source of truth into the domain model the builder consumes |
| `db/session.py` | Engine and session construction |
| `domain/budget.py` | Spend limits.""" |
| `domain/catalog.py` | Model catalog: what can be called, and where it is served.""" |
| `domain/component.py` | The component registry: what may fill a port, and what proved it can |
| `domain/finetune.py` | Fine-tuning jobs: what is asked for, and where a job has got to |
| `domain/identity.py` | The org -> team -> user -> application -> API key hierarchy |
| `domain/policy.py` | Policy: an ordered decision table over a fixed attribute set |
| `domain/scorecard.py` | Scorecards and the gate that decides whether an artifact may serve |
| `domain/signing.py` | Publisher signatures over component manifests |
| `domain/tenant.py` | What the builder needs to know about one tenant, and about the fleet.""" |
| `errors.py` | The control plane's error vocabulary |
| `finetune_main.py` | Run the fine-tune reconciler |
| `service/accounting.py` | Fold usage back into budgets |
| `service/evaluator.py` | EvalPort: the seam between a promotion gate and whatever measures a model |
| `service/finetune.py` | The fine-tune reconciler: what moves a job from a spec to an artifact |
| `service/keys.py` | Issuing, rotating and revoking API keys |
| `service/policy.py` | Writing a policy: the seam an external authority publishes rules through |
| `service/provisioning.py` | Creating the things a gateway serves: tenants, models, keys, budgets |
| `service/registry.py` | Registering components, and the gate that decides which ones are bindable |
| `service/requests.py` | Reading what requests did, for whoever has to explain one |
| `service/rollout.py` | Deciding whether a canary has earned its next step |
| `service/trainer.py` | TrainerPort: the seam between a fine-tune job and whatever runs it |
| `service/usage_stream.py` | Read usage events from the Redis stream |
| `snapshot/builder.py` | Compile the domain model into the snapshot the data plane serves from |

## Configuration

| Variable | Read by |
|---|---|
| `GATEWAY_ADMIN_HOST` | `controlplane/src/model_gateway_control/admin_main.py` |
| `GATEWAY_ADMIN_PORT` | `controlplane/src/model_gateway_control/admin_main.py` |
| `GATEWAY_ADMIN_TOKEN` | `controlplane/src/model_gateway_control/admin_main.py` |
| `GATEWAY_CONSUMER_NAME` | `controlplane/src/model_gateway_control/accounting_main.py` |
| `GATEWAY_CONTROL_PLANE_TOKEN` | `dataplane/internal/config/config.go` |
| `GATEWAY_CONTROL_PLANE_URL` | `dataplane/internal/config/config.go` |
| `GATEWAY_DATABASE_URL` | `controlplane/src/model_gateway_control/finetune_main.py` |
| `GATEWAY_FINETUNE_INTERVAL` | `controlplane/src/model_gateway_control/finetune_main.py` |
| `GATEWAY_KEY_PEPPER` | `dataplane/internal/config/config.go` |
| `GATEWAY_LISTEN_ADDR` | `dataplane/internal/config/config.go` |
| `GATEWAY_NER_SOCKET` | `dataplane/internal/config/config.go` |
| `GATEWAY_OTLP_ENDPOINT` | `dataplane/internal/config/config.go` |
| `GATEWAY_OTLP_INSECURE` | `dataplane/internal/config/config.go` |
| `GATEWAY_REDIS_URL` | `dataplane/internal/config/config.go` |
| `GATEWAY_REGION` | `dataplane/internal/config/config.go` |
| `GATEWAY_ROLLOUT_AUTOMATIC` | `controlplane/src/model_gateway_control/finetune_main.py` |
| `GATEWAY_SIGNATURE_POLICY` | `controlplane/src/model_gateway_control/admin_main.py` |
| `GATEWAY_SNAPSHOT_FILE` | `dataplane/internal/config/config.go` |
| `GATEWAY_SNAPSHOT_INTERVAL` | `dataplane/internal/config/config.go` |
| `GATEWAY_TRACE_SAMPLE_RATIO` | `dataplane/internal/config/config.go` |
| `GATEWAY_TRUSTED_KEYS` | `controlplane/src/model_gateway_control/admin_main.py` |
| `GATEWAY_WASM_DIR` | `dataplane/internal/config/config.go` |

## Decisions

| Record | Title |
|---|---|
| [`0001-polyglot-split.md`](docs/adr/0001-polyglot-split.md) | ADR 0001 — Go data plane, Python control plane |
| [`0002-layered-snapshot.md`](docs/adr/0002-layered-snapshot.md) | ADR 0002 — The snapshot is layered, not monolithic |
| [`0003-snapshot-wire-format.md`](docs/adr/0003-snapshot-wire-format.md) | ADR 0003 — Snapshot wire format is deferred to M1, and will be generated |
| [`0004-native-provider-adapters.md`](docs/adr/0004-native-provider-adapters.md) | ADR 0004 — Native Go provider adapters, not a LiteLLM sidecar |
| [`0005-no-closure-table.md`](docs/adr/0005-no-closure-table.md) | ADR 0005 — No identity closure table |
| [`0006-compiled-policy-not-a-policy-language.md`](docs/adr/0006-compiled-policy-not-a-policy-language.md) | ADR 0006 — Compile policy to a decision table; do not adopt a policy language yet |
| [`0007-pii-transform-per-attempt.md`](docs/adr/0007-pii-transform-per-attempt.md) | ADR 0007 — Transform per attempt, not per request |
| [`0008-statistical-detection-in-a-sidecar.md`](docs/adr/0008-statistical-detection-in-a-sidecar.md) | ADR 0008 — Statistical PII detection runs in a sidecar, opt in per rule |
| [`0009-component-registry.md`](docs/adr/0009-component-registry.md) | ADR 0009 — The registry gates bindings; the gate never runs in the control plane |
| [`0010-sandboxed-admission-runner.md`](docs/adr/0010-sandboxed-admission-runner.md) | ADR 0010 — The admission runner is a separate deployable that can only report |
| [`0011-wasm-in-process-components.md`](docs/adr/0011-wasm-in-process-components.md) | ADR 0011 — In-process components are WASM, with a fresh instance per call |
| [`0012-signed-manifests.md`](docs/adr/0012-signed-manifests.md) | ADR 0012 — Publisher signatures, with the trust root outside the database |
| [`0013-finetune-reconciler.md`](docs/adr/0013-finetune-reconciler.md) | ADR 0013 — Fine-tune jobs are reconciled, and the idempotency key is the safety property |
| [`0014-eval-gate.md`](docs/adr/0014-eval-gate.md) | ADR 0014 — The eval gate: integers, directions, and a baseline measured the same way |
| [`0015-weighted-rollout.md`](docs/adr/0015-weighted-rollout.md) | ADR 0015 — Weight is a share of traffic, and the walk is an operator's decision |
| [`0016-shadow-traffic.md`](docs/adr/0016-shadow-traffic.md) | ADR 0016 — Shadow traffic measures whether an adapter works, not whether it is better |
